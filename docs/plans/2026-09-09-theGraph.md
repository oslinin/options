# The Graph Subgraph + Copilot Integration: Implementation Plan

**Goal:** Replace the brute-force, capped, per-address chain scans that back
the LP Dashboard and the AI Copilot's position tools with a subgraph indexing
`AquaCollateralVault`'s events, then wire the Copilot to query it. Targets
[EthOnline2026's "Best AI Tooling or AI Use Case with The Graph (Continuity)"
bounty](https://ethglobal.com/events/ethonline2026/prizes/the-graph) — Continuity
pool, since this extends the existing `EthOnline2026_continuation_track`
branch rather than starting fresh.

**Branch:** `EthOnline2026_continuation_track` (current).

**Why this is in scope, not bounty-chasing:** the gap is real and already
found twice independently:
- `frontend/components/LPDashboard.tsx` used to show whichever authorization
  was globally most-recently-created (any LP), not the connected wallet's
  own — fixed today with a client-side `eth_getLogs` scan (see commit for
  2026-09-10's Discord thread), documented as a stopgap in
  `docs/limitations.md` L12a.
- `frontend/lib/copilot/chain.ts:112-156` (`readAuths`) is worse: it loops
  every authId from `0` to `nextAuthId`, **hard-capped at `MAX_AUTHS = 50`**
  (`chain.ts:17`). Past 50 authorizations ever created (not 50 *active* —
  50 *total*, ever), the copilot silently stops seeing new ones — including
  its own connected wallet's LP position. `readWalletPositions`
  (`chain.ts:174-248`) compounds this: for every active auth it re-scans
  `MAX_STRIKES_PER_AUTH = 40` strikes on the \$50 grid with N+1 RPC calls to
  find deployed `OptionToken`s, then N+1 more to check balances — no cap on
  correctness there, just on speed and RPC cost.

A subgraph fixes both classes of bug at once (no cap, no N+1) and gives the
Copilot a natural "live blockchain data source" story for the bounty's
AI-agent half.

**Stack:** `graph-cli` + AssemblyScript mappings (`matchstick-as` for unit
tests), local `graph-node` via Docker Compose for dev against Anvil, Graph
Studio for the Sepolia deployment judges will actually query.

## Ground rules

- Never touch `src/vaults/AquaCollateralVault.sol`'s event signatures — the
  subgraph is a pure consumer. If a field is missing from an event (see G2),
  read it via a bound contract call in the mapping, don't change the ABI.
- The `getLogs`-based fixes already shipped in `LPDashboard.tsx` and (after
  G4) `chain.ts` are correctness fallbacks for local Anvil dev, where no
  indexer runs. Never delete them — the subgraph is additive for Sepolia/
  mainnet-class deployments, not a hard dependency for `./local.sh`.
- `cd frontend && npx tsc --noEmit` clean after every task that touches
  `frontend/`. `graph codegen && graph build` clean after every task that
  touches `subgraph/`.
- New package `subgraph/` joins `pnpm-workspace.yaml`'s `packages:` list.

## Words used

- **Subgraph**: a schema + AssemblyScript mappings that a `graph-node`
  compiles, indexes from chain events, and serves as GraphQL.
- **Studio**: The Graph's hosted deploy target (`thegraph.com/studio`) —
  where the Sepolia deployment judges query lives.
- **Data source template**: a subgraph pattern for indexing contracts whose
  *addresses aren't known at deploy time* (here: each `OptionToken`, lazily
  deployed per strike by `AquaCollateralVault.buy()`) — the mapping calls
  `OptionToken.create(address)` the first time it sees that address, and a
  second template picks up that instance's own events from then on.
- **Subgraph MCP**: The Graph's hosted MCP server exposing natural-language
  queries over any of its 15,000+ indexed subgraphs — a separate, optional
  integration point from "the Copilot's own subgraph" (see G6).

## G1: Local subgraph scaffold — Authorization entity

**Files:** create `subgraph/subgraph.yaml`, `subgraph/schema.graphql`,
`subgraph/src/vault.ts`, `subgraph/tests/vault.test.ts`; modify
`pnpm-workspace.yaml`.

**Test first:** `matchstick-as` test `handleRangeAuthorized creates an
Authorization entity with active=true`; `handleAuthorizationRevoked sets
active=false without touching other fields`.

**Implement:**
- `graph init` scaffolding manually (no interactive wizard in CI): data
  source `AquaCollateralVault`, network `localhost` initially (graph-node
  can index any JSON-RPC endpoint, including Anvil — see G3 for the network
  switch to `sepolia`).
- `schema.graphql`: `type Authorization @entity { id: ID! # authId as string
  lp: Bytes! strikeMin: BigInt! strikeMax: BigInt! expiry: BigInt! isCall:
  Boolean! collateralToken: Bytes! maxCollateral: BigInt! usedCollateral:
  BigInt! active: Boolean! createdAtBlock: BigInt! createdAtTimestamp:
  BigInt! }`.
- `vault.ts`: `handleRangeAuthorized` (from `RangeAuthorized`,
  `AquaCollateralVault.sol:168`) creates the entity; `handleAuthorizationRevoked`
  (from `AuthorizationRevoked`, `:169`) loads by `event.params.authId` and
  flips `active = false`.
- ABI: copy `out/AquaCollateralVault.sol/AquaCollateralVault.json`'s ABI
  array into `subgraph/abis/AquaCollateralVault.json` (`graph codegen` needs
  it standalone, not the full Foundry artifact).
**Verify:** `graph codegen && graph build` clean; `graph test` passes.
**Commit:** `feat(subgraph): scaffold with Authorization entity from RangeAuthorized/AuthorizationRevoked`

## G2: Keep `usedCollateral` in sync via bound contract calls

**Files:** modify `subgraph/src/vault.ts`, `subgraph/subgraph.yaml`
(add `OptionBought`, `CollateralReleased`, `PullFailed` as handled events on
the same data source).

**Test first:** `matchstick-as` test using `createMockedFunction` to stub
`AquaCollateralVault.authorizations(authId)` — `handleOptionBought refreshes
usedCollateral from a contract call, doesn't reimplement the pull math`.

**Implement:**
- `RangeAuthorized` doesn't carry `usedCollateral` (starts at 0 anyway) or a
  live `active`/`maxCollateral` after partial fills — rather than
  reimplementing `AquaCollateralVault.sol`'s JIT-pull accounting in
  AssemblyScript (drift risk), `handleOptionBought`, `handleCollateralReleased`,
  and `handlePullFailed` each do one bound `AquaCollateralVault.bind(event.address)
  .authorizations(authId)` call and overwrite `usedCollateral`/`active`
  directly from the return value. One extra `eth_call` per relevant event;
  negligible at this event volume.
**Verify:** `graph test` passes; manually confirm against `cast call` output
for a live authId on Anvil (matches the value from the earlier Discord
debugging session's `cast call ... authorizations(uint256)...` command).
**Commit:** `feat(subgraph): refresh usedCollateral/active from a bound contract call, not reimplemented math`

## G3: Local graph-node against Anvil, docker-compose

**Files:** create `subgraph/docker-compose.yml`, `subgraph/README.md`;
modify `local.sh` (optional: print the local subgraph playground URL if the
container is running, don't start it automatically — it's a dev convenience,
not a dependency of the demo).

**Test first:** none (infra task) — verify via a manual GraphQL query
instead.
**Implement:**
- Standard 3-container compose (`graph-node`, `ipfs`, `postgres`) pointed at
  `host.docker.internal:8545` (or the Linux-equivalent bridge — this box is
  the VPS itself, so `http://172.17.0.1:8545` or `--network host`; document
  whichever actually works in `subgraph/README.md`, don't guess in the plan).
- `graph create --node http://localhost:8020 smile/local && graph deploy
  --node http://localhost:8020 --ipfs http://localhost:5001 smile/local`.
**Verify:** query `{ authorizations { id lp strikeMin strikeMax active } }`
against `http://localhost:8000/subgraphs/name/smile/local` and see the
authorizations already on this VPS's Anvil (authId 0 and 1 from the earlier
debugging session).
**Commit:** `chore(subgraph): local graph-node via docker-compose, indexing Anvil`

## G4: Wire the frontend to the subgraph (Sepolia + local dual-mode)

**Files:** create `frontend/lib/subgraph.ts`; modify
`frontend/components/LPDashboard.tsx`, `frontend/lib/copilot/chain.ts`,
`frontend/.env.example`.

**Test first:** none new (existing `npx tsc --noEmit` gate); manual check —
LPDashboard shows both of a test LP's active ranges once G7's stretch entity
exists, or just the correct one immediately after G4 if scoped to
Authorization only.
**Implement:**
- `NEXT_PUBLIC_SUBGRAPH_URL` env var (Studio query URL once G5 deploys;
  `http://localhost:8000/subgraphs/name/smile/local` for dev). `lib/subgraph.ts`
  exports `querySubgraph<T>(query, variables)` — a thin `fetch` POST wrapper,
  no GraphQL client dependency needed for this query volume.
- `LPDashboard.tsx`'s `refreshMyAuth` (added in the 2026-09-10 fix): when
  `NEXT_PUBLIC_SUBGRAPH_URL` is set, query
  `{ authorizations(where: {lp: $lp, active: true}, orderBy: createdAtBlock,
  orderDirection: desc) { id strikeMin strikeMax expiry isCall collateralToken } }`
  instead of the `eth_getLogs` scan. Keep the `getLogs` path as the fallback
  when the env var is unset (local Anvil dev without the G3 container
  running) — this is the "never delete the fallback" ground rule.
- `chain.ts`'s `readAuths`: same swap, dropping `MAX_AUTHS` entirely when the
  subgraph is available (the cap only exists because the current version
  can't paginate cheaply — the subgraph can).
**Verify:** `npx tsc --noEmit` clean; with `NEXT_PUBLIC_SUBGRAPH_URL` pointed
at G3's local endpoint, LPDashboard and the copilot's `get_positions` tool
both return the same authorizations the direct `cast call` shows.
**Commit:** `feat(frontend): query the subgraph for LP authorizations, getLogs as fallback`

## G5: Deploy contracts + subgraph to Sepolia (the judged deployment)

**Files:** none new; runs `forge script script/Deploy.s.sol --rpc-url
sepolia --broadcast`, `subgraph/subgraph.yaml` (network: sepolia, addresses
+ startBlock from the deploy output), `subgraph/networks.json`.

**Test first:** none (deployment task).
**Implement:**
- No prior Sepolia deployment exists (`broadcast/Deploy.s.sol/` only has a
  `31337/` directory) — this is a real first deploy, needs Sepolia ETH in
  the deployer wallet.
- `graph auth` with a Studio deploy key, `graph deploy smile-sepolia`
  (Studio, not the deprecated hosted service).
- Update `frontend/.env.example`'s `NEXT_PUBLIC_SUBGRAPH_URL` comment with
  the Studio query URL pattern; do **not** commit the actual Sepolia
  contract addresses to `.env.example` (matches existing convention — see
  `NEXT_PUBLIC_AQUA_VAULT` etc., left blank, filled by the deploy script
  locally).
**Verify:** Studio's hosted GraphQL playground returns real Sepolia data;
the deployed frontend (Vercel/wherever the Sepolia build points) shows LP
positions sourced from Studio.
**Commit:** `chore(subgraph): deploy smile-sepolia to Graph Studio`

## G6 (stretch): Long-option positions via dynamic data sources

**Files:** modify `subgraph/subgraph.yaml` (add `OptionToken` as a
`templates:` entry), `subgraph/schema.graphql`, `subgraph/src/vault.ts`,
create `subgraph/src/optionToken.ts`.

**Test first:** `matchstick-as` test `handleOptionBought instantiates an
OptionToken template exactly once per token address`; `optionToken.test.ts`:
`Transfer events update a Position entity's balance, zero-balance positions
are removed, not zeroed`.
**Implement:**
- `handleOptionBought` calls `OptionToken.create(event.params.optionToken)`
  the first time that address is seen (track via a `Bytes[]` set on the
  parent `Authorization`, or rely on the subgraph runtime's own dedup —
  confirm which before writing the guard).
- `optionToken.ts`'s `handleTransfer`: standard ERC-20 balance-per-holder
  bookkeeping into a `Position` entity keyed `${token}-${holder}`, replacing
  `chain.ts`'s `MAX_STRIKES_PER_AUTH`-bounded strike-grid + balance-fan-out
  scan entirely.
**Verify:** `graph test` passes; a Sepolia test buy shows up in
`{ positions(where: {holder: $addr}) { ... } }` without the strike grid scan.
**Commit:** `feat(subgraph): index long OptionToken positions via dynamic data sources`

## G7 (stretch, AI-tooling half of the bounty): Subgraph MCP for local dev

**Files:** create/modify `.mcp.json` (repo root, if one doesn't conflict
with the workspace's own Claude Code config), `subgraph/README.md`.

**Implement:**
- Document (not auto-configure — this is opt-in tooling for contributors,
  not part of the app) how to point Claude Code / Cursor / ChatGPT at The
  Graph's Subgraph MCP server so a developer working on this repo can ask
  "what does the Authorization entity for LP 0xf39F... look like on
  smile-sepolia" in natural language during development, without hand-writing
  GraphQL. This is the bounty's other eligible path ("tooling that makes The
  Graph easier to use from AI environments") — cheap to add once G5's
  subgraph exists, strengthens the submission without new product surface.
**Verify:** a natural-language query against the Subgraph MCP correctly
resolves `smile-sepolia` and returns G5's live data.
**Commit:** `docs(subgraph): document Subgraph MCP usage for contributors`

## Milestones

| | Demoable | Tasks | Target |
|---|---|---|---|
| M1 | Local subgraph answers "LP X's active ranges" against Anvil, matches `cast call` | G1-G3 | Day 2 |
| M2 | LPDashboard + copilot `get_positions` read from the local subgraph, `getLogs` still works with it stopped | G4 | Day 3 |
| M3 | Same data, live on Sepolia via Graph Studio — the actual judged artifact | G5 | Day 4 |
| M4 (stretch) | Copilot answers "what options do I hold" without the strike-grid scan | G6 | Day 5 |
| M5 (stretch) | README documents Subgraph MCP setup for contributors | G7 | Day 5 |

**Cut order if time compresses:** G6 and G7 first (both stretch); G5 is the
one task that cannot be cut — an Anvil-only subgraph is not judgeable. If G5
itself is at risk (no Sepolia ETH, Studio friction), ship M1-M2 and be
explicit in the submission that the subgraph is proven against a public
network's event shape but not yet deployed there.

## Definition of done

- G1-G5 merged: a live Graph Studio subgraph on Sepolia, `LPDashboard.tsx`
  and `chain.ts` reading from it with the existing `getLogs`/RPC-scan paths
  kept as the local-Anvil fallback (never deleted).
- `docs/limitations.md` L12a rewritten from "no indexer" to "indexer exists
  for Sepolia/mainnet-class deployments; local Anvil dev still uses the
  bounded scan since running `graph-node` isn't part of `./local.sh`."
- `frontend`: `npx tsc --noEmit` clean; `subgraph`: `graph codegen && graph
  build` and `graph test` clean.

## Docs to update on completion

- `docs/limitations.md`: rewrite L12a per "Definition of done" above.
- `docs/solutions.md`: new entry for the subgraph (check the current highest
  `S` number at implementation time — `S13` is reserved by
  `docs/plans/2026-09-05-aqua.md` for
  MarginVault; use the next free number, don't collide).
- `README.md`: document the Copilot (currently undocumented entirely — flagged
  independently in the 2026-09-10 Discord thread) and the subgraph, including
  how to point a local `.env.local` at either the Sepolia Studio endpoint or
  a locally-run `graph-node`.
- `docs/reference-table.html` and `frontend/lib/copilot/tools.ts`: update the
  `L1-L12`/`S1-S12` range comments once the new entries land (same pattern
  the MarginVault plan calls for).
- Regenerate: `cd frontend && pnpm run gen-help && node scripts/gen-knowledge.mjs`,
  commit the outputs (same as every other doc-affecting change in this repo).

---

# Phase 2 (2026-09-11): from indexer to trading agent

**Status of phase 1:** G1–G5 shipped (Studio `smile-sepolia` v0.0.2, LP
dashboard + copilot reading it, RPC fallback kept). G6/G7 were cut.
Judged as-is this is "used The Graph properly", not "built AI tooling on
it" — the bounty text rewards *trading and execution agents, portfolio
copilots, risk monitors* and *tooling that makes The Graph easier to use
from AI environments (MCP servers, agent SKILLs, client configs)*. Phase 2
makes the subgraph the spine of a trading copilot and adds the tooling
half. Deadline: 2026-09-13 12:00.

**Two facts that shape the design**

- The Graph's network registry lists `arc-testnet` (eip155:5042002) with
  the `subgraphs` service, so the same subgraph can be deployed to Studio
  for Arc as well as Sepolia. "On Arbitrum" only means the *publish*
  transaction (Studio → decentralized network) happens on Arbitrum One;
  the indexed chain stays Sepolia/Arc.
- This VPS is arm64: no local `graph-node`. Anvil therefore gets a
  **dev tape** — the same entities rebuilt from `eth_getLogs` — gated on
  chain id 31337/1337 only. On any public chain the copilot's Graph tools
  have **no RPC path**: that is what makes the subgraph load-bearing, in
  code, not wording.

## P1: Load-bearing, in code

**Files:** `frontend/lib/copilot/chain.ts`, `frontend/lib/subgraph.ts`,
`frontend/lib/tape.ts` (new), `frontend/app/api/subgraph/route.ts` (new),
`frontend/components/LPDashboard.tsx`.

- `readAuths`: the RPC scan runs only when `isLocalChain(chainId)`. On
  Sepolia/Arc with no subgraph URL the tool returns
  `{ error: "subgraph required on public networks" }` — never a capped scan.
- `lib/tape.ts`: `readFills`, `readPositions`, `readSeries`, `readAuths`
  with one `source` tag per result (`"subgraph"` | `"anvil-logs"`). Every
  copilot answer built on them cites the source.
- `/api/subgraph`: server-side proxy so a gateway URL with an API key
  (`SUBGRAPH_URL`, server env) never reaches the browser.
  `NEXT_PUBLIC_SUBGRAPH_URL` (Studio dev endpoint) keeps working as before.
**Commit:** `feat(copilot): subgraph is the only position source on public chains; dev tape for Anvil`

## P2: Subgraph — Series and Position entities

**Files:** `subgraph/schema.graphql`, `subgraph/subgraph.yaml`,
`subgraph/src/vault.ts`, `subgraph/tests/vault.test.ts`,
`subgraph/networks.json` (+ `arc-testnet`).

- `Series` (id = optionToken): authorization, lp, strike, expiry, isCall,
  openInterest, fillCount, volume, lastPremiumPerUnit, lastTradeAt. Open
  interest by strike in one query.
- `Position` (id = optionToken-holder): holder, series, balance. `+` on
  `OptionBought`, `−` on `OptionClosed` and `Redeemed` (new handler). ERC-20
  transfers of option tokens are still not tracked (needs G6 templates —
  documented, not built).
- Redeploy `smile-sepolia` v0.0.3; create + deploy `smile-arc-testnet`
  (`--network arc-testnet`, address + startBlock from
  `deployments/arc-5042002-base-stack.json`).
**Commit:** `feat(subgraph): Series (open interest, last trade) and Position (holder balance) entities; arc-testnet network`

## P3: Copilot Graph tools (subgraph-only on public chains)

**Files:** `frontend/lib/copilot/tools.ts`, `frontend/lib/copilot/graphTools.ts`
(new), `frontend/lib/copilot/deribit.ts` (new), `frontend/lib/copilot/macro.ts` (new).

| Tool | Source | Does |
|---|---|---|
| `find_opportunities` | fills + series + auths, Deribit | ranks live series: Smile ask vs Black-Scholes at the reference vol, vs last traded premium; labels cheap / expensive |
| `liquidity_map` | auths + series | per $50 band × call/put × expiry: capacity, used %, open interest, days since last trade; flags scarce, empty, stale |
| `portfolio_greeks` | positions (holder) + series (LP short side) | net delta/gamma/vega/theta for the wallet's long and written positions, marks, P&L vs cost |
| `hedge_suggestion` | math only | quantity of spot ETH, short calls at K, or short puts at K that brings a book to delta-neutral |
| `reference_market` | Deribit public API | nearest-listed ETH option IV for a strike/expiry, DVOL, index price; 60 s cache |
| `macro_calendar` | hardcoded | upcoming FOMC/CPI-type dates in N days + the event-vol heuristics the macro skill teaches |
| `get_positions` | tape | switched to Position entity — the strike-grid scan is gone on public chains |
| `prepare_lp_range` | UI tool | card: strikes, expiry, size, collateral, expected premium; "Open in Write a Range" prefills the form |
| `prepare_rfq_quote` | UI tool | card: authId, strike, size, premium/unit inside the formula ask, ttl; "Open in RFQ desk" prefills the signer |

The agent prepares; the user signs. No key ever leaves the wallet.
**Commit:** `feat(copilot): opportunities, liquidity map, portfolio greeks, hedging, Deribit reference vol, macro calendar, LP/RFQ preparation`

## P4: Skills

**Files:** `frontend/skills/*.md` (new), `frontend/scripts/gen-knowledge.mjs`,
`frontend/lib/copilot/systemPrompt.ts`, `frontend/components/copilot/SkillsMenu.tsx`
(new), `CopilotPanel.tsx`, `subgraph/SKILL.md` (new), `.mcp.json.example` (new).

- Built-in skills (SKILL.md format: frontmatter `name`, `description`,
  `starter`): trading-opportunities, risk-management, delta-hedging,
  explain-margin, lp-market-making, rfq-quoting, macro-context,
  calendar-spreads. Bundled by gen-knowledge; enabled set lives in
  localStorage and rides the request context; enabled bodies are injected
  as "## Active skills".
- **Skills button** in the panel: list with toggles + starter prompt per
  skill + "Add a skill" (paste markdown, stored locally) — the user can
  add calendar spreads or anything else without a rebuild.
- `subgraph/SKILL.md`: the agent-facing doc for the subgraph itself
  (entities, canonical queries, endpoints per network, the dev tape) so a
  Claude Code / Cursor user can drop it in and query `smile-sepolia`.
- `.mcp.json.example`: The Graph Subgraph MCP client config.
**Commit:** `feat(copilot): skills menu with built-in trader skills and user-added skills; subgraph SKILL.md`

## P5: MCP — the copilot talks to The Graph's Subgraph MCP and any server the user adds

**Files:** `frontend/app/api/copilot/route.ts`, `frontend/lib/copilot/mcp.ts` (new),
`frontend/components/copilot/CopilotSettings.tsx`, `frontend/package.json` (`@ai-sdk/mcp`).

- Settings gains an MCP servers list (name, URL, bearer token), localStorage
  only, sent per request in a header like the BYOK key; one preset button:
  **The Graph Subgraph MCP** (`https://subgraphs.mcp.thegraph.com/sse`,
  token = a Gateway API key). Server env `COPILOT_MCP_SERVERS` seeds the same
  list for the hosted demo.
- The route opens each server per request, merges its tools with the built-in
  ones, closes them when the stream finishes.
**Commit:** `feat(copilot): MCP servers per user, The Graph Subgraph MCP preset`

## P6: Chart — option premium and IV over time with the underlying

**Files:** `frontend/components/PriceChart.tsx`, `frontend/lib/tape.ts`.

- Series picker built from the tape (most-traded first). Premium per unit as
  a line on its own price scale; implied vol at each fill (Black-Scholes
  inverted in the browser, spot = candle close at that hour) as a second
  line. Underlying candles stay.
**Commit:** `feat(ui): traded premium and implied vol per series on the price chart`

## P7: A tape to look at — 100 trades on Anvil, a seed for Sepolia

**Files:** `script/SeedTape.s.sol` (new), `script/seed-tape.sh` (new), `local.sh`.

- Three ranges (two call expiries, one put), then `N` buys and a few
  sellbacks at pseudo-random strikes and sizes. On Anvil `seed-tape.sh`
  spreads them over simulated days (`evm_increaseTime`) and random-walks the
  mock oracle so premiums and IV actually move. `local.sh` runs it
  (`SEED_TRADES=100`, `0` to skip). On Sepolia the same script with the
  deployer keys and no time warping.
**Commit:** `feat(script): seed a trade tape — 100 Anvil trades on ./local.sh, Sepolia variant`

## P8: Publish, keys, Arc

- Studio → **Publish** `smile-sepolia` to the network (Arbitrum One gas;
  no curation signal needed — the upgrade indexer serves it). Create a
  Gateway API key; set `SUBGRAPH_URL` (server) to the gateway URL. Same for
  `smile-arc-testnet`.
- Wizard in `subgraph/README.md`; the two steps that need a wallet are the
  user's.

## P9: Docs — the story in the help files

`docs/copilot.md` (tools, skills, MCP, sources), `docs/guide.md` (asking the
copilot for opportunities, hedges, a range, a quote), `docs/limitations.md`
L12a ("lifted by the subgraph; the 50-range cap is gone on public
networks"), `README.md` (feature table rows, sponsor map, the 50-cap
sentence up front), `docs/submission-ethonline2026.md` (Graph section
rewritten around the agent + tooling halves), `docs/video-walkthrough-instructions.md`
(Graph segment: opportunities → hedge → prepare a range), `subgraph/README.md`,
`Story.tsx` (one line: indexed by The Graph, no range cap). Help site and
knowledge pack regenerate on build.

## Status (2026-09-11, evening)

| Task | Status | Commit |
|---|---|---|
| P1 load-bearing in code | done — `lib/tape.ts`, `/api/subgraph`, RPC scan gated to Anvil | `38f8922`, `3ae5997` |
| P2 Instrument + Position entities | done — `smile-sepolia` v0.0.4, `smile-arc-testnet` v0.0.1 on Studio (`Series` renamed `Instrument`: graph-node pluralises it as `series_collection`) | `91882e4`, `d04e594`, `a2bf596` |
| P3 copilot tools | done — verified end-to-end on the seeded Anvil tape: `find_opportunities` (live hook sigma per expiry), `liquidity_map` → `prepare_lp_range` card, `portfolio_greeks` → `hedge_suggestion` | `5f1b9b5`, `eb13431`, `b5efb24`, `77ed83e` |
| P4 skills | done — eight `frontend/skills/*.md`, Skills menu, `subgraph/SKILL.md`, `.mcp.json.example` | `39e9a41` |
| P5 MCP | done — `@ai-sdk/mcp`, per-user servers, Subgraph MCP preset; a bogus server is skipped non-fatally; the Graph MCP itself untested (needs a Gateway key) | `00cbbe3` |
| P6 chart | done — premium + IV per instrument, own scales, tape source in the legend | `9235276` |
| P7 tape | done — `./local.sh` seeds 100 trades over 54 simulated hours with a random-walked oracle (83 buys, 17 sellbacks, 3 ranges) | `79135eb` |
| P8 publish + API key + Arc | Arc done; **publish + Gateway API key are the user's wallet steps** — wizard in `subgraph/README.md` | — |
| P9 docs | done | `175a32e` |

matchstick tests are written but the binary has no arm64 build; they run
on x86 only.

## Cut order if time compresses

P5 (MCP) and `prepare_rfq_quote` first, then P6's IV line (keep premium),
then P8's Arc deployment (keep Sepolia). P1–P3, P7 and P9 are the
submission.
